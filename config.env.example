# Configurações do Servidor
PORT=3100
NODE_ENV=development

# Configurações do MongoDB Atlas
# Opção 1: Usar variáveis individuais
DB_USER=seu_usuario_mongodb
DB_PASS=sua_senha_mongodb
DB_NAME=portal-aluno

# Opção 2: Usar URI completa (recomendado para Vercel)
# MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/nome-do-banco?retryWrites=true&w=majority

# Configurações JWT
# IMPORTANTE: Use uma chave forte e única em produção (mínimo 32 caracteres)
JWT_SECRET=senac-portal-aluno-secret-key-2024-mude-esta-chave-em-producao
JWT_EXPIRES_IN=7d

# Configurações CORS (separe múltiplas origens por vírgula)
# Para desenvolvimento local:
CORS_ORIGIN=http://localhost:5173,http://localhost:3000
# Para produção no Vercel, adicione a URL do seu frontend:
# CORS_ORIGIN=https://seu-frontend.vercel.app,https://seu-dominio.com

# URL do Frontend (para links em emails)
FRONTEND_URL=http://localhost:5173
# Em produção:
# FRONTEND_URL=https://seu-frontend.vercel.app

# Configurações SMTP para envio de emails
# Para Gmail:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-app-password-aqui
# IMPORTANTE: Para Gmail, você precisa criar uma "App Password":
# 1. Ative a verificação em duas etapas na sua conta Google
# 2. Vá em: https://myaccount.google.com/apppasswords
# 3. Gere uma senha de app e use aqui

# Para outros provedores SMTP, ajuste as configurações acima
# Exemplo Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
